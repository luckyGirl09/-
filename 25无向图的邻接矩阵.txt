图的存储结构：图没有顺序存储结构，但可以借助二维数组来表示元素间的关系（邻接矩阵）

邻接矩阵
建立一个顶点表（记录各顶点信息）和一个邻接矩阵（表示各个顶点间的关系）
图的邻接矩阵是一个二维数组，如果有n个顶点，那么矩阵就有n行n列
如果有边，则A（i,j）=1，没有则为0.

①无向图的邻接矩阵是对称的
②顶点i的度 = 第i行（列）中1的个数
③完全图的邻接矩阵中，对角元素为0其余为1

